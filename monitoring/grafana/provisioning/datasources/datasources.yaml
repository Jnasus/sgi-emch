apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus-sgi          # UID fijo: el dashboard lo referencia directamente
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false

  - name: Loki
    type: loki
    uid: loki-sgi                # UID fijo: el dashboard lo referencia directamente
    access: proxy
    url: http://loki:3100
    editable: false
